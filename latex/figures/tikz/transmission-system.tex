\documentclass[tikz]{standalone}

\input{preamble}

\begin{document}
	\begin{tikzpicture}[
		node distance=10pt,
        block/.style={draw, very thick, fill=white, minimum height=28pt, minimum width=58pt, text width=52pt, align=center},
        arrow/.style={-Latex},
	]
		\node[block] (src) {Information source};
		\node[block, right=of src] (tx) {Transmitter};
		\node[block, right=of tx] (ch) {Channel};
		\node[block, right=of ch] (rx) {Receiver};
		\node[block, right=of rx] (dst) {Information destination};
		\node[block, below=of ch] (ns) {Noise source};
		
		\draw[arrow] (src) -- (tx);
		\draw[arrow] (tx) -- (ch);
		\draw[arrow] (ch) -- (rx);
		\draw[arrow] (rx) -- (dst);
		\draw[arrow] (ns) -- (ch);
	\end{tikzpicture}
\end{document}
